spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/niladriSen007/banking-system-centralized-config
          username: niladriSen007
          password: ${GIT_ACCESS_TOKEN}
          search-paths: global,transaction-service
          clone-on-start: true
          default-label: main
server:
  port: 8888